<UserControl x:Class="xinglin.Views.TemplatePreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:xinglin.Views"
             xmlns:models="clr-namespace:xinglin.Models.CoreEntities"
             Width="800"
             Height="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <ToolBar Grid.Row="0">
            <Button Content="打印"/>
            <Button Content="导出"/>
            <Button Content="关闭" Click="CloseButton_Click"/>
        </ToolBar>
        
        <!-- 预览区域 -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1">
            <ScrollViewer>
                <Canvas x:Name="PreviewCanvas" Background="White"
                        Width="794"
                        Height="1123">
                    <!-- 模板内容将在这里显示 -->
                </Canvas>
            </ScrollViewer>
        </Border>
        
        <!-- 状态栏 -->
        <StatusBar Grid.Row="2">
            <TextBlock Text="预览模式"/>
        </StatusBar>
    </Grid>
</UserControl>