<UserControl x:Class="xinglin.Views.TemplateViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:xinglin.Views"
             xmlns:vm="clr-namespace:xinglin.ViewModels"
             Width="800"
             Height="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 工具栏 -->
        <ToolBar Grid.Row="0">
            <Button Content="加载模板"/>
            <Button Content="导出"/>
        </ToolBar>
        
        <!-- 模板显示区 -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1">
            <ScrollViewer>
                <StackPanel>
                    <TextBlock Text="{Binding CurrentTemplate.Name}" FontWeight="Bold" FontSize="16" Margin="10"/>
                    <TextBlock Text="{Binding CurrentTemplate.Description}" Margin="10"/>
                    <!-- 模板内容显示 -->
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>